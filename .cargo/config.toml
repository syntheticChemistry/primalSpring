# SPDX-License-Identifier: AGPL-3.0-or-later
# Lean release binaries: LTO + single codegen unit + stripped symbols.
# Sovereign binary reproducibility per ecoBin standard.

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[profile.release-with-debug]
inherits = "release"
strip = false
debug = true
