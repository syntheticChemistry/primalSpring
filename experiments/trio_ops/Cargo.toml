# SPDX-License-Identifier: AGPL-3.0-or-later
[package]
name = "primalspring-trio-ops"
version = "0.9.16"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Neural API helpers for provenance trio experiments (not part of primalspring core types)"

[dependencies]
primalspring = { path = "../../ecoPrimal" }
serde_json = { workspace = true }

[lints]
workspace = true
